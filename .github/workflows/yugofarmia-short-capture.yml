name: Yugofarmia short-video live capture

on:
  pull_request:
    branches:
      - main

permissions:
  contents: read

jobs:
  capture:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Install Playwright
        run: |
          npm install --no-save playwright@1.55.0
          npx playwright install --with-deps chromium

      - name: Capture current Yugofarmia data and assets
        run: node yugofarmia-short/capture.mjs

      - name: Upload live capture
        uses: actions/upload-artifact@v4
        with:
          name: yugofarmia-short-live-capture
          path: yugofarmia-short/out
          if-no-files-found: error
          retention-days: 2
